<% if ilike = current_user.likes.find_by(post_id: single.id) %>
  <%= link_to unlike_tag, ilike, method: :delete %>
<% else %>
  <%= link_to like_tag, likes_path(id: single.id), method: :post %>
<% end %>